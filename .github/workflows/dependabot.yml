name: dependabot
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: langston-barrett/gha/.github/workflows/merge-dependabot-prs.yml@585d8ef2d04787e911c2ff3bfbda30e5b3d620f3
    with:
      only-patch: false
