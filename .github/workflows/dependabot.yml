name: dependabot
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: langston-barrett/gha/.github/workflows/merge-dependabot-prs.yml@6292a6a380c64b0d40ee22384fb96d21bc74667c
    with:
      only-patch: false
